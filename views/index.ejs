<%- include('partials/header') %>


    <h1>Home</h1>

    <% folders.map((folder) => {%>
        <div>Folder: <%= folder.name %>  </div>
        <div>Files: 
            
            <% folder.files.map((file) => {%>
                <%= file.name %>
            <%}) %>  
        
        
        </div>
    <%}) %>
    
<%- include('partials/footer') %>